services:
  db:
    image: postgres:15
    restart: always
    environment:
      - POSTGRES_USER=steepskynet
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=axum-db
    ports:
      - "5432:5432"